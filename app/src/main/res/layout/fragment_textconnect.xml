<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <android.support.design.widget.TabLayout
        android:id="@+id/tabs_textconnect"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/tabsborder"
        app:tabIndicatorColor="@color/colorPrimary"
        app:tabTextAppearance="@style/KofCTabTextAppearance"
        app:tabIndicatorHeight="4dp"
        app:tabMode="fixed"
        app:tabGravity="fill"
        app:tabSelectedTextColor="@color/colorPrimary"/>


    <android.support.v4.view.ViewPager
        android:id="@+id/view_pager_textconnect"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_gravity="bottom"/>

<!--
    <android.support.design.widget.FloatingActionButton
        android:id="@+id/floating_action_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top"
        app:fabSize="normal"
        app:layout_anchor="@id/navigation"
        app:layout_anchorGravity="top|end"
        app:layout_behavior=".BottomNavigationFABBehavior"
        app:srcCompat="@drawable/ic_add_white_24dp"
        app:useCompatPadding="true"/>
-->

</LinearLayout>